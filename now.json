{
  "name": "occasio-partners",
  "alias": "occasio-partners.now.sh",
  "version": 2,
  "builds": [
    { "src": "static/**", "use": "@now/static" },
    { "src": "www/package.json", "use": "@now/static-build" }
  ],
  "routes": [
    { "src": "^/static/(.*)", "dest": "/static/$1" },
    { "src": "^/(.*)", "dest": "/www/$1" }
  ],
  "env": {
  }
}
