{
  "name": "occasio-partners",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "analyze": "BUNDLE_ANALYZE=browser next build",
    "build": "next build",
    "dev": "next",
    "export": "next export",
    "now-build": "yarn test && yarn build && yarn export -o dist",
    "test": "echo \"No tests specified!\""
  },
  "babel": {
    "presets": [
      "next/babel"
    ],
    "plugins": [
      "preval"
    ]
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "^7.0.2",
    "next-compose-plugins": "^2.1.1",
    "next-offline": "^3.2.1",
    "react": "16.6.0",
    "react-dom": "16.6.0",
    "reaptcha": "^1.4.0"
  },
  "devDependencies": {
    "@zeit/next-bundle-analyzer": "^0.1.2",
    "babel-plugin-preval": "^3.0.1",
    "file-loader": "^2.0.0"
  }
}
